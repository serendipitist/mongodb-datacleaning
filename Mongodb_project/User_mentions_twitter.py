#who included the most user Mentions?#$unwind operator- will produce a individual document for every user mentions from pymongo import MongoClientimport pprintclient =MongoClient("mongodb://localhost:27017")db=client.examplesdef user_mentions():    result=db.tweets.aggregate([{"$unwind":"$entities.user_mentions"},                                {"$group":{"_id":"$user.screen_name","count":{"$sum":1}}},                                {"$sort":{"count":-1}},                                {"$limit":1}])    return resultif __name__ == 'main':    result=user_mentions()    pprint.pprint(result)    print result        